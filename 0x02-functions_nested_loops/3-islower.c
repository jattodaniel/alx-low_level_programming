#include "main.h"
/**
 * _islower - function that checks the lowercase characters
 * @c - single letter input
 * Return: always success
 */
int _islower(int c)
{

if (c >= 'a' && c <= 'z')

return(1);

else

return(0);
}
